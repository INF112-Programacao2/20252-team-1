#include "menu.h"

/*Menu::Menu(sf::RenderWindow &window)
    : Room(window),
      _manager(*static_cast<RoomManager *>(nullptr)),
      _start_button(window, "Start", sf::Vector2f(300, 150), sf::Vector2f(200, 50), sf::Color::Green, sf::Color::White),
      _exit_button(window, "Exit", sf::Vector2f(300, 250), sf::Vector2f(200, 50), sf::Color::Red, sf::Color::White),
      _credits_button(window, "Credits", sf::Vector2f(300, 350), sf::Vector2f(200, 50), sf::Color::Blue, sf::Color::White),
      _options_button(window, "Options", sf::Vector2f(300, 450), sf::Vector2f(200, 50), sf::Color::Yellow, sf::Color::White)

      //std::string text, sf::Font &font, int font_size, std::function<void()> on_click_callback, Room &room
{
    if (!_font.loadFromFile("assets/arial.ttf")) {
        // Tratar erro de carregamento da fonte
    }
}*/